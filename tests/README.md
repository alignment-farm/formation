# Deterministic contract tests

The current 177 non-contact tests include 162 tests covering the fixture-local
prefix,
first condition append,
typed admitted-root boundary, public replay-constraint append, and shared
positive-foreground delivery, positive encounter opening, and the two-root
positive activation decision, semantic practice-request construction, and
deterministic actor invocation, and positive action commitment. They
establish byte identity where selected, semantic capability identity where
bytes remain unselected, validation, handoffs, authority separation, fork and
information-flow boundaries, and once-only transitions. They establish no
request serialization, real-model contact, environment action application,
consequence production, ablation replay result, behavioral influence, or
formation effect.

The other 15 tests cover the isolated revision micro-environment's prospective
98-case transition matrix and frozen refusal witnesses. Its external oracle
verifies that results depend on supplied state as well as action. These tests
are a computation specimen, not another formation-runtime slice or formation
evidence.

Ten tests cover the calibration information-gap specimen. They establish that
identical public foreground admits opposite hidden calibrations, one consequence
identifies a two-slot mapping, new device tokens prevent action copying, target
direction changes the required token, another family remains underdetermined,
and current state can make calibration irrelevant. They spend no model calls
and establish no developmental effect.

Twelve fake-contact tests cover the SQLite charter runner without spending its
model-call budget. They verify prompt assembly, executable oracles, engagement
and stop decisions, exact nineteen-call scheduling, raw-offer separation, and
retry receipts. Ten more cover the strict Python boundary runner, including its
source gate, fresh-process oracle, exact vector order, automatic stop, and full
nineteen-call path. Six more cover the local small-model admission runner,
including its frozen schedules, ordinary-versus-boundary distinction,
classifiers, full fake packet, and anchor stop. Twelve additional fake-contact
tests cover the corrected Nemotron-only successor, including its disclosed
builtin environment, exact source and query gates, vector-owned class labels,
direct-rule constraint, full packet, retry, request/load identity, and anchor
stop.

Nine fake-contact tests cover the Gemma 270M-to-1B structured-action
staircase. They lock prompt bytes, documented oracles, artifact identities,
type-sensitive JSON scoring, refusal precedence, retry behavior, live request
and load shape, two-model continuation, packet receipts, and cleanup.

Thirteen tests cover the paired bare-versus-grammar structured-output trial.
Ten cover the Granite computation gate, and nine cover the Qwen computation
gate. They preserve each completed contact's exact interface, scheduling,
scoring, retry, and receipt boundaries without spending another model call.

Twelve tests cover the bounded exploratory developmental contact. They verify
the exact Docker model and request surface, strict action parsing, offer
separation, interpreter information limits, rotating schedule, physical-call
budget, transport-only retry, exact request retention, and full continuation
through wrong or invalid later behavior without spending a model call.

Twelve tests cover the two-world calibration mechanism-discovery contact. They
lock dynamic action parsing, transition-derived candidate governance, mirrored
hidden calibrations, changed-token schedules, governed deactivation identity,
harness-label exclusion, full 69-call continuation, unwarranted-guess capture,
refused-candidate continuation, and transport-only retry without spending a
model call.

Six tests cover the single Muse counter-prior successor. They bind the exact
larger Docker artifact, preserve all consumed cases and assignments, disclose
the model-native token and instruction adjustments, exclude harness labels,
complete the full fake successor, and stop an unobservable interface without
opening another model loop.

Six tests cover the explicit environment consequence renderer. They verify
factual selected-slot and movement-direction fields for controls, calibration
requests, and holds while keeping the inferred opposite-slot rule and all
experimental fields out of the result.

Eight tests cover the final explicit-consequence contact. They lock fresh
mirrored worlds, field-only candidate derivation with no numeric fallback,
changed-token schedules, governed deactivation identity, harness-label
exclusion, full fake continuation, wrong-action retention, and the final stop
boundary.

Nineteen tests cover the phase-coupled deterministic specimen. They exhaust
both profiles, phases, and applicable target directions; freeze the exact
occurrence and action surfaces; refuse copied and malformed commitments; keep
warrant scoring outside the environment; and establish no model behavior or
developmental effect.

Sixteen fake-contact tests cover the phase-coupled exploratory runner without
spending its model budget. They freeze prompt and case surfaces, strict parsing,
candidate refusal, ablation authorization, rotations, request-equivalence
classes, full 69-call continuation, and acquisition stopping.

Fifteen fake-contact tests cover the occurrence-accounting exploratory runner
without spending its model budget. They freeze the fresh worlds and prompts,
strict account and candidate parsing, exact content-control bytes, conservative
account diagnostics, two-round lineage and request identity, conditional
omissions, stability and withheld gates, malformed and token-disparity paths,
transport retry, and the explicit live flag.

Nineteen fake-contact tests cover the exact-draft challenge runner. They freeze
the source identity, exact prompts and schedules, raw malformed and unavailable
source handling, nested response order, all equivalence collapses, withdrawal,
withheld carryover invalidity, provider and local prompt-token parity,
source-blind mass construction, actual inspect-template bindings, delayed
governance, transport-only retry, evidence integrity, and the explicit live
flag without spending a participant-model call.

Sixteen fake-contact tests cover the executable-prediction revision runner.
They reproduce the frozen case and witness artifacts, keep awkward scalar
outputs executable, exercise the three-valued rule evaluator, freeze request
and material variants, preserve unavailable paths and denominators, verify
byte-identical repeats with distinct identities, test transport-only retry and
early stopping, and detect evidence changes without spending a participant-
model call.

Fourteen tests cover the unselected-lineage deterministic specimen. They
exhaust both opaque family profiles and target directions, distinguish absent
from available-empty proposals, freeze all four later case roles before any
simulated action, separate pre-result occurrence from environment result,
construct all six foreground paths, prove exposed/ablation and cold-request
identity, and enforce complete 96-cell scoring with refusal witnesses. They use
no participant model and establish no behavioral or Formation result.

Twenty-six fake-contact tests cover the unselected-lineage behavior runner. They
reconstruct the published manifest and witness, bind prompt and render
surfaces, keep provider content, JSON parsing, commitment, and environment
physics separate, exercise all 109 assigned calls and six paths, preserve full
denominators through unavailable and exhausted-provider cases, enforce the
disposable and live-CLI stops, validate rendering before transport, bind every
exact delivered and hidden path, and regenerate evidence from raw attempts.
They also prove module-form no-contact launch and pure live-argument
construction under fail-on-use side-effect canaries. They spend no participant-
model calls.

Six fake-contact tests cover the same-request variation runner. They bind four
canonical request bodies to the retained packet, freeze the 32-call interleaved
schedule, retain exact requests and per-request action distributions, limit
transport retry, preserve malformed outputs, and refuse evidence-directory
overwrite. They spend no participant-model calls.

The full locked-environment run passes 443 tests and 362 subtests with
`uv run pytest -q`.
