[project]
name = "formation"
version = "0.0.0"
description = "Research apparatus for governed development around a cold model"
requires-python = ">=3.14,<3.15"
dependencies = [
    "jinja2==3.1.6",
    "tokenizers==0.23.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
]

[tool.pytest.ini_options]
pythonpath = ["."]
