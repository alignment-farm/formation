# Phase-coupled control proposal review

Status: **two independent final confirmations returned `PROPOSAL_STABLE`; the
deterministic specimen is licensed; model contact is not**.

## What was reviewed

The reviews asked whether the phase-coupled domain leaves every later action
with the cold model while freezing enough deterministic structure to detect
information leakage, answer-shaped action surfaces, and hidden runtime policy.
The declared ceiling was kept narrow: application of one situated binary family
profile, not acquired procedural competence.

## Review history

| Round | Exact model identifier | Terminal result | Material issue |
|---|---|---|---|
| Initial mechanism review | `composer-2.5` | `REVISE_PROPOSAL` | Narrow the claim, freeze the operating rule and acquisition identifiability, remove answer-shaped candidate fields, and separate fixed foreground from divergent trajectories. |
| Initial mechanism review | `cursor-grok-4.6-high-fast` | `REVISE_PROPOSAL` | Freeze distinct controls, bounds, action shape, identifier opacity, and distance-two commitment semantics. |
| Revised mechanism review | `composer-2.5` | `REVISE_PROPOSAL` | Add canonical occurrence bytes, exact action schemas, content ablation, unobserved-family scoring, and executable specimen obligations. |
| Revised mechanism review | `cursor-grok-4.6-high-fast` | `REVISE_PROPOSAL` | Remove remaining case and slot leaks; freeze no-feedback success geometry and harness-only boundaries. |
| Specimen-readiness review | `composer-2.5` | `PROPOSAL_STABLE` | No blocking flaw. |
| Specimen-readiness review | `cursor-grok-4.6-high-fast` | `REVISE_PROPOSAL` | Conditional `hold` availability could reveal a harness-only case class through the permitted-action list. |
| Final confirmation | `composer-2.5` | `PROPOSAL_STABLE` | The uniform action-list repair closed the leak; no blocking flaw. |
| Final confirmation | `cursor-grok-4.6-high-fast` | `PROPOSAL_STABLE` | The uniform action-list repair closed the leak; no blocking flaw. |

One attempted `cursor-grok-4.6-high` session in the initial round ended without
a response. It produced no verdict and was transparently retried with the exact
available identifier `cursor-grok-4.6-high-fast`.

## Exact final review question

Both final confirmations received this same question through Cursor `agent` in
read-only `ask` mode:

> You are an independent causal-design reviewer. Work read-only: do not edit
> files. Read AGENTS.md, README.md, docs/PROCEDURAL_INFLUENCE_PROBLEM.md,
> docs/PROCEDURAL_DOMAIN_SELECTION.md, and the current
> docs/PHASE_COUPLED_CONTROL_PROPOSAL.md. This is a final confirmation after the
> previous review found that conditional hold availability could leak case
> type. The proposal now freezes every one-action permitted list as the two
> public controls in order followed by hold, while every commitment list
> contains only the two public controls; it also states that no persistence
> uses the common envelope with material null. Decide whether the current
> proposal licenses only a pure deterministic specimen—not model contact—and
> whether any causal-design decision remains missing that could let the
> specimen smuggle a policy or expected answer. Treat implementation checks
> already captured by the nine executable specimen obligations as nonblocking.
> Do not require this one-bit domain to prove acquired procedural competence;
> its ceiling is model-mediated application of situated family-profile
> knowledge. Return exactly one terminal verdict, PROPOSAL_STABLE or
> REVISE_PROPOSAL. Before it, name any blocking flaw with precise file/line
> references. End with a final line exactly TERMINAL_VERDICT: &lt;verdict&gt;.

## What stability licenses

The stable verdict licenses implementation and testing of the nine pure
deterministic specimen obligations in the proposal. It does not license a case
schedule, model choice, inference settings, budget, exploratory charter, model
request, or finding. Any later charter must begin from the specimen results,
not from the reviewers' confidence.
