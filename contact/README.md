# Model-contact runners

This directory contains narrow executors for bounded research questions. A
runner may assemble stated prompts, invoke the named cold model, execute an
external oracle, and retain evidence. It may not interpret experience for the
runtime or turn an exploratory observation into a Formation finding.

Exploratory runners need enough prospective detail to make their evidence
interpretable: question, exact model and interface, observation of interest,
budget, stopping condition, and evidence destination. They do not require a
separate chain of mechanism, implementation, contact-decision, and model-review
documents. Claim-bearing validation may freeze a stronger contract after
exploration has found a thesis-relevant phenomenon.

Failures before a participant request are engineering failures. Record and
repair them, but do not count them as scientific contacts or use them to create
a new research route. Participant contact still requires human authorization.

The first runner implements the
[SQLite contact charter](../docs/SQLITE_CONTACT_CHARTER.md). Its tests use fake
participant outputs and spend no model-contact budget.

The second runner implements the
[strict Python boundary contact](../docs/PYTHON_BOUNDARY_CONTACT_CHARTER.md).
It accepts one restricted function, runs every frozen input in a fresh child
process, and stops after exploration unless the predeclared boolean-as-integer
contrast appears. Its tests also use fake participant outputs.

The third runner implements the
[small-model admission exploration](../docs/MODEL_ADMISSION_EXPLORATION.md).
It verifies two exact local GGUF artifacts and their embedded chat templates,
loads each without vision or speculative decoding, and admits only a model that
is reliable on ordinary work while showing a repeatable, directly teachable
boundary gap. Its tests use fake inference envelopes.

The fourth runner implements the
[corrected Nemotron successor](../docs/NEMOTRON_ADMISSION_SUCCESSOR.md) without
rewriting the first packet. It exposes the complete restricted Python
environment to the model, uses fresh prompts and vectors, and stores ordinary
and held class labels on the vectors themselves. Its tests spend no model-call
budget.

The fifth runner implements the
[Gemma structured-action staircase](../docs/GEMMA_CONTRACT_STAIRCASE.md). It
screens exact 270M and 1B artifacts on four JSON computations before either can
earn a full admission packet. It scores only provider message content, never
reasoning text, and its tests use fake envelopes.

The sixth runner implements the
[paired structured-output interface trial](../docs/STRUCTURED_OUTPUT_INTERFACE_TRIAL.md).
It sends fresh tasks through bare and shape-only JSON-schema conditions, checks
that `response_format` is the sole request difference, and classifies structure
separately from answer correctness. Its tests spend no model-call budget.

The seventh runner implements the
[Granite computation gate](../docs/GRANITE_COMPUTATION_GATE.md). It tests one
exact text-only artifact on four fresh constrained computations. Passing only
earns a later admission charter; its tests use fake envelopes.

The eighth runner implements the [Qwen computation gate](../docs/QWEN_COMPUTATION_GATE.md).
It binds a complete MLX package plus hub thinking controls, permits only text
strings on the multimodal request surface, and uses fresh fake-tested tasks.

The ninth runner implements the completed
[exploratory developmental contact](../docs/EXPLORATORY_DEVELOPMENTAL_CONTACT.md).
It uses one exact Qwen3 14B Docker Model Runner artifact, one minimal JSON action
surface, the unchanged revision-gated release environment, and four later
offers under a hard physical-call ceiling. Wrong or variable behavior is
retained rather than used as an admission stop. Its fake-contact tests spend no
model calls. The [live evidence](../evidence/exploratory-developmental-contact-20260817/README.md)
contains 27 completed calls and no Formation verdict.

The tenth runner implements the
[calibration mechanism-discovery contact](../docs/CALIBRATION_MECHANISM_DISCOVERY.md).
It contacts two mirrored hidden calibrations with new action tokens on every
later device, compares no persistence, raw occurrence, exact authored
interpretation, and a minimal public-scope gate, and retains wrong or awkward
behavior under a fixed 72-attempt ceiling. Its tests use fake provider output.
The [live evidence](../evidence/calibration-mechanism-discovery-20260817/README.md)
contains 69 completed calls and no Formation verdict.

The eleventh runner implements the single
[Muse counter-prior successor](../docs/MUSE_COUNTER_PRIOR_SUCCESSOR.md). It
reuses the consumed two-world calibration packet with one exact larger Docker
artifact and disclosed model-native token and instruction adjustments. It is an
exploratory operational replication, not admission or validation.
It stopped after one HTTP 500 because the provider did not recognize the exact
model architecture. Its [evidence](../evidence/muse-counter-prior-successor-20260817/README.md)
contains no model action or behavioral result.

The twelfth runner implements the final
[explicit-consequence contact](../docs/EXPLICIT_CONSEQUENCE_CONTACT.md). It uses
fresh mirrored Qwen worlds and environment-issued selected-slot and movement
facts while preserving candidate authorship and opposite-slot inference as
model responsibilities. It is the stopping representation variant, not a new
validation packet. The [live evidence](../evidence/explicit-consequence-contact-20260817/README.md)
contains 69 completed calls and the direction-asymmetric influence observation.

The thirteenth runner implements the reviewed phase-coupled exploratory charter.
It uses the conforming two-phase specimen, exact null/raw/authored/governed and
same-family ablation offers, one frozen 69-call schedule, and an external warrant
oracle. Sixteen tests use fake provider output, and two independent conformance
reviews licensed the single completed live contact. The
[evidence](../evidence/phase-coupled-exploratory-contact-20260818/README.md)
contains 69 completed calls, two refused token-copying candidates, no available
candidate-content comparison, and null Formation and validation verdicts.

The fourteenth runner implements the reviewed occurrence-accounting authorship
charter. It freezes two fresh worlds, one shared model-authored account per
observable world, eight two-sample candidate conditions, exact-byte equality,
the direct/withheld audit, conditional omissions, and a 40-attempt ceiling. Its
15 tests use fake provider output. Composer 2.5, Grok 4.6, and Kimi K3 returned
`RUNNER_CONFORMS`. The single completed
[evidence record](../evidence/occurrence-accounting-exploratory-contact-20260818/README.md)
contains 37 calls, no available delivery-conditioned comparison, and null
Formation and validation verdicts.

The fifteenth runner implements the reviewed exact-draft challenge charter. It
retains the predeclared first direct output string without repair, freezes all
four direct samples before downstream construction, runs nine two-sample
conditions per observable world, applies the hash-verified Docker inspect chat
template to a source-blind prompt-mass search, and delays lexical governance
until authorship ends. Its 19 fake-contact tests spend no participant calls.
Composer 2.5 and Grok 4.6 returned `RUNNER_CONFORMS`. The single live contact is
complete. Its
[evidence record](../evidence/draft-challenge-exploratory-contact-20260818/README.md)
contains 39 calls, no revision or withdrawal label, and null Formation and
validation verdicts.

The sixteenth runner implements the reviewed executable-prediction revision
charter. It keeps every scalar model string executable, evaluates optional
model-authored conditional rules without choosing actions, separates prediction
trials from environment consequences, and preserves unavailable outputs in a
fixed 35-call, two-world packet. Its fake-contact tests reproduce the frozen
case and witness hashes and spend no participant-model call. The single live
contact is complete. Its
[evidence record](../evidence/executable-prediction-revision-contact-20260818/README.md)
contains 35 calls, one mixed World K candidate event, substantial call
variation, and null Formation and validation verdicts.

The seventeenth runner materializes the unselected-lineage behavior contact. It
derives the published live witness from the manifest, keeps JSON
parsing separate from action commitment and environment physics, continues all
six information paths through a 109-call fake packet, and regenerates evidence
from raw responses. Its live Docker adapter has no default execution path. The
first file-path command failed during top-level import, before `main()` or any
participant request. The later module-form repair passed 26 focused tests and
the full 437-test suite. After explicit user authorization, the runner completed
all 109 participant calls without retry. The
[evidence account](../evidence/unselected-lineage-behavior-contact-20260819-contact/README.md)
records one differing transfer cell and a byte-identical control disagreement
of the same size. Formation and validation verdicts remain null.

The earlier documents treated the import failure as consuming a one-use
scientific authorization. That interpretation is retained as research-process
history but is no longer the working method. It explains an engineering repair,
not a finding about the thesis and not a template for future contacts.

The eighteenth runner measures same-request variation exposed by that contact.
It reads four exact canonical request bodies from the retained evidence,
interleaves eight repeats of each, and reports each request's action
distribution under a 32-call logical budget and 36-attempt physical ceiling.
Its tests and smoke path spend no participant-model calls. After explicit user
authorization, the live contact completed all 32 calls without retry. The
[evidence account](../evidence/same-request-variation-20260819T231848Z/README.md)
records two actions for the earlier disagreement request and one observed
action for each of the other three requests. Formation and validation verdicts
remain null.

The nineteenth runner implements the resulting distributional developmental
comparison. One fresh acquisition and its external result produce one
model-authored candidate. Authored delivery, authored ablation, raw occurrence,
and a static family rule then continue into two same-family cases and one
opposite-profile non-transfer case. Each branch-case request is repeated eight
times, making the action distribution the comparison unit. Seven fake-contact
tests cover the 98-call schedule, fresh opaque specimen, exact repeated request
bytes, malformed-output continuation, bounded retry, raw-evidence replay, and
no-contact smoke path. The completed
[evidence](../evidence/distributional-developmental-20260819T233903Z/README.md)
contains 98 calls. All four conditions produced identical action distributions,
so the bounded result is null.

The twentieth through twenty-third runners tested retained-material
consumption, clause order, canonical mapping authorship, and a scoped
continuation. Generic consumption instructions had no effect. A canonical
first-then-second rule changed 15 of 16 same-family decisions, and a model
exposed to the external consequence authored that exact rule. The effect
repeated in fresh same-family cases, while the first narrow non-transfer case
did not distinguish scoped from ungated delivery. Each runner retains raw
evidence and a replayed account under `evidence/`.

The twenty-fourth runner froze the canonical mapping candidate for a two-world,
258-call validation. Exposed candidates were exact and correct; withheld
candidates were exact opposites. Governed, ungated, and static delivery each
changed all 24 transfer decisions, while cold, raw, withheld, and ablated paths
changed none. All branches passed the upward non-transfer cases, so the frozen
verdict was null: governance did not outperform the simpler authored lesson.
The [evidence](../evidence/canonical-mapping-candidate-validation-20260820T000646Z/README.md)
retains the complete comparison.

The twenty-fifth runner widened non-transfer sampling. Both candidates left all
32 upward cases intact but reversed 31 of 32 downward cases. The twenty-sixth
runner then froze an exact-family scope-gate successor. Scoped delivery blocked
all eight tested downward errors, but matching upward delivery changed only two
of eight decisions. Its frozen verdict is `not_engaged`; the
[evidence](../evidence/canonical-mapping-scope-gate-validation-20260820T001909Z/README.md)
set the preceding representation-robustness boundary.

The twenty-seventh runner compares three model-authored representation classes
after the same kind of external consequence: a relation sentence, an effect
table, and a target policy. Result-exposed and consequence-withheld authorship
continue into repeated matching cases; ungated exposed forms also continue into
both nonmatching directions. The completed 190-call
[evidence](../evidence/representation-class-exploration-20260820T010342Z/README.md)
is null. Sentence and table authorship worked with complete later influence in
one world and reversed in the other. A correct policy in the second world had
no later influence. The result separates authorship fidelity from downstream
consumption.

The twenty-eighth runner isolates later consumption by supplying known-correct
sentence, effect-table, and policy lessons across four fresh controller
families, both target directions, and unrelated opposite-rule families. The
completed 256-call
[evidence](../evidence/representation-consumption-calibration-20260820T112303Z/README.md)
selects the sentence and effect table: each scored 32/32 on matching calls,
while empty retention scored 0/32 and the policy scored 6/32. Both selected
forms harmed some unrelated cases without the family gate. This is an
interface calibration, not a Formation result.

The twenty-ninth runner tests lesson authorship alone across eight fresh
experiences. Direct sentence and table forms stop null. The thirtieth runner
inserts one model-written direct observation before table authorship. After a
mechanical scorer correction against the unchanged frozen specification, its
[evidence](../evidence/staged-observation-authorship-20260820T115153Z/README.md)
records 24/24 staged tables, 9/24 direct tables, and 0/24 after observation
removal. A fresh 344-call action successor then scores 16/16 matching and 32/32
gated unrelated actions but remains null because raw history solves one frozen
cell. Its [evidence](../evidence/staged-observation-action-chain-20260820T115845Z/README.md)
selects a stricter four-world validation.

The thirty-first runner is that staged action successor. The thirty-second
runner executes the four-world validation: exact staged lessons score 29/32 on
matching action and preserve 31/32 unrelated actions, but its frozen verdict is
null. The thirty-third runner balances both possible controller relations
across six worlds. Exact staged lessons score 33/36 matching, versus 18 cold,
18 raw, 12 consequence-hidden, 2 direct, and 18 after removal. One difficult
upward cell leaves that frozen verdict null. Their
[four-world evidence](../evidence/staged-chain-validation-20260820T121232Z/README.md)
and [balanced evidence](../evidence/balanced-relation-staged-validation-20260820T122857Z/README.md)
retain the complete comparisons.

The thirty-fourth runner converts the same staged observation into both a
relation sentence and effect table. Across six fresh balanced worlds, both
model-written forms and both supplied controls score 36/36 matching actions.
The formal table preference is caused by a flawed threshold that rewards more
ungated harm, so the [evidence](../evidence/staged-representation-consumption-20260820T124630Z/README.md)
supports both matching-action interfaces rather than a substantive table
advantage.

The thirty-fifth runner performs the corrected eight-world aggregate
validation. All exposed observations and tables are exact; the staged branch
scores 48/48 matching, preserves the cold unrelated score, and passes every
frozen causal comparison. Its
[evidence](../evidence/staged-chain-aggregate-validation-20260820T130635Z/README.md)
supports the bounded mechanism while keeping Formation null.

The thirty-sixth runner is the prospective two-phase revision exploration. It
verifies an old table before reversing the environment relation, records a new
model-written observation, retains both table versions in lineage, and compares
the revised table with stale, raw, hidden, removed, ungated, cold, and supplied
controls. Its fake-contact tests spend no participant calls.

The thirty-seventh runner expands revision to eight balanced worlds. Exact
revisions score 48/48 post-change and pass every frozen causal comparison. Its
[evidence](../evidence/staged-table-revision-validation-20260820T134318Z/README.md)
supports bounded revision while keeping Formation null.

The thirty-eighth runner acquires two tables in sequence per lineage. Gated
delivery keeps both useful at 44/48, but joint delivery scores 36/48 and favors
the first family. The thirty-ninth runner reuses those exact tables and changes
only their container. Reversing list order shifts the favored family; a keyed
prompt object scores 44/64 against 63/64 for external gating. The
[accumulation evidence](../evidence/staged-table-accumulation-20260820T140646Z/README.md)
and [container evidence](../evidence/accumulated-table-container-20260820T142422Z/README.md)
set the applicability-versus-identity boundary that selected the next route.

The fortieth runner introduces a separate Qwen3 4B clerical model with
restricted sensory and scope inputs. Its first full comparison is not engaged:
classification reaches 10/12, while one-step effect encoding and the new JSON
delivery form fail. The forty-first runner supplies known-correct records and
finds a usable short-sentence interface at 32/32 actions.

The forty-second runner stages sensory transcription, effect prose, later
normalization, and structural matching. Transcription is exact and matching
avoids recombination errors, but reversed sentence order blocks participant
use. The forty-third runner tests named effect fields and finds that direct
causal completion is still unreliable. The forty-fourth asks the clerk only to
parse two facts already explicit in its prose. All four records are exact and
the composed branch reaches its frozen pipeline-candidate verdict.

The forty-fifth runner performs the fresh four-lineage validation. It balances
the visible features across opposite effects, runs complete exposed and hidden
clerical chains, and compares cold, raw, removal, unselected, normalized,
oracle, matching, novel, and recombined paths. The full pipeline scores 48/48
matching, preserves unrelated action, and passes the frozen learned-instrument
validation. The
[evidence](../evidence/learned-clerical-instrument-validation-20260820T171748Z/README.md)
keeps Formation null and selects revision as the next problem.

The forty-sixth runner preserves old and new clerk records as linked versions
while the environment reverses four relations. The exposed clerk writes four
exact revisions, and newest-version delivery makes 24/24 post-change matching
actions. Stale and removed revisions make 0/24. The frozen exploratory verdict
is `revision_candidate`. Its hidden-consequence control also reveals that the
clerk invents complete records when a needed measurement is missing, which
selects source-grounded admission as the next instrument boundary. The
[evidence](../evidence/learned-clerical-revision-20260820T174848Z/README.md)
retains all 404 calls and an exact replay.

The forty-seventh runner is a zero-call provenance diagnostic. It binds each
revision proposal to the exact sensory request and source occurrence, admits
all four complete sources, and quarantines all four missing-movement sources.
The forty-eighth runner asks the 4B clerk for a broader source-support verdict.
All outputs are valid, but it accepts 9/12 stale opposite records and stops
null.

The forty-ninth runner decomposes that failed judgment into selected-field
projection. It is exact on 48/48 calls across both actuator positions, both
effects, stale records, and hidden-source records. The fiftieth runner makes no
model calls and composes provenance, exact sensory copying, learned projection,
and exact effect equality. It admits 24/24 supported retained records and
quarantines 24/24 unsupported ones. The
[provenance](../evidence/source-grounded-revision-admission-20260820T180358Z/README.md),
[verifier](../evidence/clerical-source-support-verifier-20260820T180818Z/README.md),
[projection](../evidence/clerical-selected-effect-projection-20260820T181202Z/README.md),
and [composition](../evidence/composed-clerical-record-admission-20260820T181541Z/README.md)
accounts route next to a fresh end-to-end validation.

The fifty-first runner performs that fresh four-lineage validation. All eight
old and revised records and projections are exact. Composed admission accepts
all supported versions, quarantines all stale and hidden proposals, and makes
48/48 matching actions while preserving unrelated behavior. Its frozen verdict
is `not_engaged` because one raw-history control cell returns invalid old tokens
on all three repeats and the global validity rule vetoes the apparatus. The
[evidence](../evidence/composed-clerical-revision-validation-20260820T183033Z/README.md)
retains the formal verdict and routes to one prospectively corrected engagement
successor.

The fifty-second runner is that successor. It uses four new lineages and the
same 768-call mechanism, comparisons, thresholds, and fixed denominators. Only
the engagement witness changes: supplied-guidance and composed-admission cells
must be valid, while invalid output in any control branch remains wrong without
vetoing the apparatus. All 768 calls completed without retry. Admitted revisions
made 45/48 matching actions, tied supplied revisions, and lost all matching
benefit under removal. The frozen bounded verdict is `supported`. Its fake tests
also prove that the preceding evidence still replays under the original scorer.

The fifty-third runner continues the exact supported parent packet instead of
recreating it. It keeps two admitted version-2 records per lineage, returns only
scope A to its version-1 relation, proposes a source-bound version 3, and tests
whether scope B remains at version 2. Its 688-call schedule includes stale,
hidden, removal, supplied, raw, cold, and unrelated comparisons. Fake tests
bind the parent hash, chronology, selective version assignments, budget,
replay, and no-contact default.

All 688 calls completed without retry. The runner bound every parent version,
produced four exact admitted version-3 records, assigned all 72 later versions
correctly, made 45/48 matching actions, preserved B at 24/24, and preserved the
cold unrelated score. Its frozen verdict is `null` because only three of four
third-occurrence actions followed version 2's target policy. All four results
nevertheless contradicted version 2's effect claim for the selected control.

The fifty-fourth runner makes no model calls. It binds that exact packet and
reconstructs the selected slot, retained effect claim, target-recommended
action, and external movement for each source occurrence. The observation-
grounded governor admits 4/4; the action-attributed governor admits 3/4. Its
conformance result establishes a clean prospective policy fork without
changing the completed null.

The fifty-fifth runner binds the supported version-2 parent and creates two
public source-action paths. One participant chooses an action for a target from
version 2. One deterministic runtime exploration commits the first valid
control without knowing its effect. The same clerk and composed gate process
both consequences before observation-grounded and action-attributed governors
make separate decisions. Its 636-call later comparison tests both governors,
raw exploration, cold action, removal, supplied guidance, unchanged scope B,
and unrelated cases.

All 636 model calls and four deterministic exploratory occurrences completed
without retry. Both source origins produced 4/4 exact equal records. The
observation-grounded governor admitted every exploratory revision and made
48/48 later matching actions. The action-attributed governor quarantined them,
so it retained B at 24/24 and made A at 0/24; removal produced the same split.
Unrelated action stayed at cold. The frozen bounded verdict is `supported`.

The fifty-sixth runner makes no model calls. It preserves four exact
counterevidence histories and applies one declared observation-grounded
accumulation policy. Repeated contradiction supersedes, self-correction retains
the current record, isolated contradiction suspends pending corroboration, and
contested movement suspends unresolved. Every decision cites the ordered source
receipts it considered.

The fifty-seventh runner carries those histories through the restricted clerk
and later participant. Its 480-call schedule contains six deterministic public
explorations, 24 clerk calls, 24 later normalizations, and 432 participant
calls. Fake tests freeze the histories, generalized governor, exact schedule,
raw-evidence replay, and no-contact default.

All calls completed without retry. The clerk wrote five exact complete records
and one complete-looking record from disputed movement. The source gate admitted
the five grounded records and quarantined the disputed one. All governance
states and later catalog assignments were exact. Governed delivery made 42/48
matching actions, but the recovered record tied removal at 6/6 in its A cases.
The frozen verdict is `null`.

The fifty-eighth runner makes that recovery comparison identifiable. Two fresh
worlds acquire opposite relations for one visible scope, experience a transient
opposite observation, and recover their current record. Their later public
devices are byte-identical across worlds. The 324-call schedule compares cold,
recovered, removal, and supplied delivery on eight matching and four unrelated
cases.

All calls completed without retry. The six clerk records and both recovery
histories were exact. Cold and removal requests were byte-identical across the
opposite worlds and made 24/48 matching actions. Recovered and supplied requests
were byte-identical within world and made 48/48. The frozen verdict is
`supported`.

The fifty-ninth runner makes no model calls. It compares immediate revision
with two consecutive confirmations across five six-step uncertain histories.
The governors receive only public ordered reports; hidden relation fields stay
in the scorer. Immediate revision delivers six wrong records and suspends once.
Two-confirmation delivers no wrong record but suspends eleven times. The exact
replay verdict is `conforms` with the finding `tradeoff_exposed`.

The sixtieth runner makes no model calls. It attaches external movement,
information, delay, and action counts to five suspension responses in a
mirrored two-control world. Current, newest, cold, and explore policies all
reach the target because either task action is reversible and reveals the
hidden relation. The exact replay finding is
`symmetric_action_information_equivalence`.

The sixty-first runner makes no model calls. It verifies an asymmetric device
where the same public task action succeeds in one hidden profile and fails
irreversibly in the other. Diagnostic action preserves the device and emits a
profile signal; hold preserves it without information. The exact replay
finding is `asymmetric_probe_domain_available`.

The sixty-second runner uses that domain for a 222-call clerk and participant
contact. The clerk sees only two old diagnostic and task consequences and
writes both signal records. A fresh diagnostic result then selects the learned
record for a separate participant. Learned and supplied records complete 24 of
24 matching tasks; removal and raw history complete 12; reversed records fail
all 24. Held no-signal cases select nothing. The frozen verdict is `supported`.

The sixty-third runner gives the participant at most two self-directed actions
across known and unfamiliar diagnostic signals. Learned and supplied catalogs
complete 17/18 mapped episodes, but cold and removal already probe first. The
learned path guesses on all six unfamiliar signals and fails three. Its frozen
verdict is `harmful`.

The sixty-fourth runner exposes the deterministic match result as an explicit
record-ID receipt. Known-signal action is perfect, but changed shared wording
makes the full, silent, and no-record unfamiliar controls almost always hold.
The required unsafe pressure is absent, so the frozen verdict is `not_engaged`.

The sixty-fifth runner restores the exact harmful participant interface. All
72 parent control request hashes match. Full catalog and silent absence again
guess on all six unfamiliar signals and fail three. Empty learned, supplied,
and reversed receipts are byte-identical and hold 6/6. Known learned and
supplied receipts complete 18/18; reversed receipts fail 18/18. The frozen
verdict is `supported`.

The sixty-sixth executable makes no model calls. It conforms the public
alphabet and service-window world needed for a later knowledge-by-cost test.
Costly diagnostics consume the same public window on covered and uncovered
devices; free diagnostics preserve it; first-action hold is terminal; and
exact-match receipts distinguish one covered record from an empty uncovered
result. Its exact replay finding is
`knowledge_cost_interaction_world_available`.
