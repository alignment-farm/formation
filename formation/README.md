# Fixture-local formation code

This directory currently contains only the runtime-owned half of the first
materialization slice: adapting the fixture's six recorded receipts, emitting
their frozen artifact, and issuing a current-run handoff.

It is not yet a general formation runtime. See
[`docs/MATERIALIZATION.md`](../docs/MATERIALIZATION.md).
